const shapes = require("./shapes");

describe("Circle", () => {
  it("should ", () => {
    // arrange step
    // act step
    // assert step
  });
});

describe("Triangle", () => {
  it("should ", () => {
    // arrange step
    // act step
    // assert step
  });
});

describe("Square", () => {
  it("should ", () => {
    // arrange step
    // act step
    // assert step
  });
});
